/**
 * [RoomAddExpensePage]
 * - 지출(결제건) 등록 페이지
 * - 영수증 이미지 업로드 또는 수동 입력
 * - 결제자 / 참여자 선택
 * - 저장 시 해당 방의 지출 목록에 추가
 */
export default function RoomAddExpensePage() {
  return (
    <div style={{ padding: 16 }}>
      <h2>Add Expense</h2>
    </div>
  );
}
